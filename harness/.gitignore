# Playwright / Node build + run artifacts (regenerated by `npm run traces`).
node_modules/
test-results/
playwright-report/
blob-report/
playwright/.cache/

# Keep ./samples — those trace.zip files are the committed output of this phase.

# Benchmark run output (a per-run snapshot — see harness/benchmark.py).
benchmark_results.json
