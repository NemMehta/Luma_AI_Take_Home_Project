{
  "name": "harness-playwright",
  "version": "0.0.0",
  "private": true,
  "description": "Phase 1 trace harness — generates passing and failing Playwright traces.",
  "scripts": {
    "test": "playwright test",
    "traces": "playwright test || true; node collect-traces.mjs"
  },
  "devDependencies": {
    "@playwright/test": "1.61.0"
  }
}
